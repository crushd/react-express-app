const router = require("express").Router();

// const userRoutes = require("./user-routes.js");
// const propertyRoutes = require("./property-routes.js");
// const leaseRoutes = require("./lease-routes.js");
// const orderRoutes = require("./workorder-routes.js");

// Routes separated by table.  This prevents cross routing and confusion.
// Api-routes files is user for a better visual reference of what is happening with each route listed below, but not used in the program.
    // router.use("/users", userRoutes);
    // router.use("/properties", propertyRoutes);
    // router.use("/leases", leaseRoutes);
    // router.use("/workorders", orderRoutes);

module.exports = router;
